#include<stdio.h>
#include<locale.h>
#include<stdlib.h>

int main(int argc, char const *argv[])
{
    setlocale(LC_ALL, ".utf8");
    // soma, subtrai, multiplica e divide numeros complexos
    return 0;
}