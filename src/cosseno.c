#include<stdio.h>
#include<locale.h>

int main(int argc, char const *argv[])
{
    setlocale(LC_ALL, ".utf8");
    double x;
    printf("Digite um valor de x: ");
    scanf("%lf", &x);

    double cos_x = 1;
    for (int i = )
    return 0;
}